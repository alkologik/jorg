package jorg.jorg;

import suite.suite.Subject;

import java.io.*;
import java.net.URL;
import java.util.function.BiConsumer;
import java.util.function.Function;

public class Jorg {

    /*public static final Object terminator = new Object();

    public static<T> T read(String filePath) {
        return new JorgReader().read(filePath);
    }

    public static<T> T read(File file) {
        return new JorgReader().read(file);
    }

    public static<T> T read(InputStream inputStream) {
        return new JorgReader().read(inputStream);
    }

    public static<T> T parse(String jorg) {
        return new JorgReader().parse(jorg);
    }

    public static JorgReader withRecipe(Function<Subject, Object> recipe) {
        return new JorgReader().withRecipe(recipe);
    }

    public static JorgReader withTypedRecipe(Class<?> type, Function<Subject, Object> recipe) {
        return new JorgReader().withTypedRecipe(type, recipe);
    }

    public static<T> JorgReader withReformer(Class<T> type, BiConsumer<T, Subject> reformer) {
        return new JorgReader().withReformer(type, reformer);
    }

    public static JorgReader withAdapter(String s, Object o) {
        return new JorgReader().withAdapter(s, o);
    }

    public static boolean write(Object object, String filePath) {
        return new JorgWriter().write(object, filePath);
    }

    public static boolean write(Object object, File file) {
        return new JorgWriter().write(object, file);
    }

    public static boolean write(Object object, URL url) {
        return new JorgWriter().write(object, url);
    }

    public static String encode(Object o) {
        return new JorgWriter().encode(o);
    }

    public static<T> JorgWriter withPerformer(Class<T> type, Function<T, Subject> performer) {
        return new JorgWriter().withPerformer(type, performer);
    }

    public static JorgWriter withPort(Object object, String id) {
        return new JorgWriter().withPort(object, id);
    }*/
}
